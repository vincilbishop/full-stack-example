# https://devcenter.heroku.com/articles/build-docker-images-heroku-yml
build:
  docker:
    web: Dockerfile
run:
  web: yarn --cwd /usr/src/app/api run db:migrate && yarn --cwd /usr/src/app/api run db:seed && yarn --cwd /usr/src/app/api run start:prod
